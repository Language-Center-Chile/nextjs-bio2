Voy a aplicar las correcciones para evitar el error de "valor nulo en columna name" y asegurar que la validación funcione correctamente.

### Pasos de implementación:

1.  **Verificar y corregir `ProfileForm.tsx`**:
    *   Revisaré si la validación `if (!form?.name?.trim())` está correctamente ubicada dentro de `handleSubmit`.
    *   Si no está o está mal colocada (permitiendo el envío), la corregiré para que **detenga** el envío si el nombre está vacío.

2.  **Verificar `route.ts`**:
    *   Confirmaré que la función `clean` esté implementada.
    *   Aseguraré que el manejo de `name` sea consistente (aunque la validación del frontend es la barrera principal).

3.  **Verificación**:
    *   Comprobaré que el código compile correctamente.

Una vez confirmado, procederé a aplicar los cambios.